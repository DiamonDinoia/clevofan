clevofan-dkms (1.1+diamon3) unstable; urgency=medium

  * Reload the module on install: the old Juno package's postinst ran
    modprobe and its prerm ran rmmod, dh_dkms dropped both, and dkms 3.4.3
    does not load modules itself. An upgrade unloaded the running module
    and nothing brought it back, leaving fan control off until a manual
    modprobe. debian/clevofan-dkms.postinst now modprobes on configure,
    guarded so an unsupported mainboard (-ENODEV) logs one line and does
    not fail the install.
  * Remove the obsolete /etc/fancontrol.d/fancontrol-template conffile
    left behind by 0.4.0-2 via debian/clevofan-dkms.maintscript, so dpkg
    no longer warns that /etc/fancontrol.d is not empty on upgrade.

 -- Marco Barbone <mbarbone@flatironinstitute.org>  Thu, 24 Sep 2026 11:30:02 -0400

clevofan-dkms (1.1+diamon2) unstable; urgency=medium

  * Always assign hwmon_dev in the platform probe.
  * Serialize fan state between sysfs writes and the PM notifier.
  * Restore EC auto mode on unload only after sysfs and the notifier are gone.
  * Reject pwm_enable values other than 0, 1 and 2 with -EINVAL.
  * Pin the hwmon channel to tachometer register pairing in check-source.sh.
  * Correct the packaging text and README to what the code matches.
  * Give P170SM-A the 2 fans its board name was meant to get.

 -- Marco Barbone <mbarbone@flatironinstitute.org>  Wed, 23 Sep 2026 17:04:06 -0400

clevofan-dkms (1.1+diamon1) unstable; urgency=low

  * Repackage this fork as clevofan-dkms via dh_dkms (--with dkms),
    replacing Juno's hand-rolled postinst/prerm (cp into /usr/src, raw
    dkms add/install/remove) and its unserialized EC port I/O.
  * Fixes the V5xTNC_TND_TNE second fan: control index 2, tach
    0xD2/0xD3, the generic mapping every board uses (Juno 0.4.0-2 maps
    it to index 4, 0xD4/0xD5).
  * Sends every EC access through the ACPI EC driver (ec_transaction()
    for fan commands, ec_read() for tachometers), so this driver no
    longer races the ACPI EC driver's own port I/O.

 -- Marco Barbone <mbarbone@flatironinstitute.org>  Mon, 21 Sep 2026 13:11:12 -0400
